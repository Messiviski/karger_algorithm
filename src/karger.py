from graph import Graph


class Karger:
    def __init__(self, graph: Graph) -> None:
        self.graph = graph

    def run(self, exec_number: int, iterations_per_exec: int) -> int:
        print(1)
